姓名：构建自定义安装包

姓名：构建自定义安装包:
repository_dispatch:
类型: [build-apk]

工作:
建立:
连续的: ubuntu-最新

步骤:
    - 姓名: 签出代码
uses: 操作/结帐@v4

-
uses: 操作/设置-Java@v4
和……一起:
分布'17'
java版：: 'temurin'

替换占位符: 替换占位符
跑: |
        # 用 sed 命令查找并替换文件中的占位符
SED-i"s|{{DEFAULT_UID}}|${{github。事件。client_payload.UID}}|g"app/src/main/java/com/tencent/open/agent/AgentActivity。Java
SED-i"s|{{RESOURCE_API_URL}}|${{github。事件。client_payload.resource_api}}|g"app/src/main/java/com/tencent/tim/MainActivity。Java
SED-i"s|{{CLOUD_POPUP_URL}}|${{github。事件。client_payload.popup_api}}|g"app/src/main/java/com/tencent/tim/MainActivity。Java
SED-i"s|{{APP_NAME}}|${{github。事件。客户端有效负载.应用程序名称}}|g"app/src/main/res/values/strings.xml
SED-i"s|{{ACTIVITY_LABEL}}|${{github。事件。client_payload.activity_label}}|g"app/src/main/AndroidManifest.xml
SED-i"s|{{MAIN_TITLE}}|${{github。事件。client_payload.main_title}|g"app/src/main/res/layout/activity_main.xml

    - 姓名: 授予gradlew的执行权限
跑: chmod+x gradlew

-姓名：构建安装包
跑: ./gradlew装配释放

-姓名: 将安装包作为工件上传
uses: 操作/上载工件@v4
和……一起:
姓名: app-release
路径: app/build/outputs/apk/release/*.apk
